engine.name=$(nodeGroupId)-$(externalId)

db.driver=com.mysql.jdbc.Driver
db.url=jdbc:mysql://172.20.10.101/sym?tinyInt1isBit=false

db.user=root
db.password=root

registration.url=http://172.20.10.10:31415/sync/server

group.id=client
external.id=machine01

# Don't muddy the waters with purge logging
job.purge.period.time.ms=7200000

# This is how often the routing job will be run in milliseconds
job.routing.period.time.ms=5000
# This is how often the push job will be run.
job.push.period.time.ms=10000
# This is how often the pull job will be run.
job.pull.period.time.ms=10000
# Kick off initial load
initial.load.create.first=true